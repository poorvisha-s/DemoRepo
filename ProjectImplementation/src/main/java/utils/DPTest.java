package utils;

import java.io.FileInputStream;
import java.util.HashMap;

import org.apache.poi.xssf.usermodel.XSSFRow;
import org.apache.poi.xssf.usermodel.XSSFSheet;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;

public class DPTest {
	
	public void dpTestMethod(){
	/*	String[][] testdata = new String[2][2];
		testdata[0][0] = "Test01";
		HashMap<Integer, String> testD = new HashMap<Integer, String>();
		testD.put(0, "Test01");
		
		FileInputStream fip = new FileInputStream("./test data/TC001.xlsx");
		XSSFWorkbook wb = new XSSFWorkbook(fip);
		XSSFSheet sheet = wb.getSheetAt(0);
		int rowCount = sheet.getLastRowNum();
		int columnCount = sheet.getRow(0).getLastCellNum();
		testdata = new String[rowCount][columnCount];
		for(int i=1;i<=rowCount;i++){
			XSSFRow row = sheet.getRow(i);
			for(int j=0;j<columnCount;j++){
				 String cellData  = row.getCell(j).getStringCellValue();
				testD.put(key, value) cellData;
			}
		}
		return testdata;
		
	
*/	}

}
